> I want to create a Nodejs Backend using Express and postgresORM
> Connect local DB with PGADMIN4
> follow .env way and make utils folder and one config folder
> config will have db.config.js and
> utils will have db.js -> sequlize connectivity
> app folder - includes
> app with mvc structure but for now for view we are using react vite so model controllers middleware
> routes and main index.js
> npm init
> explain me all parser cookie , url encoded and query params and body parser
> endpoint for all of above
> REST API endpoint for user table with role table and linkage between them
> enpoint and middleware for
> /login /logout /checkauth /refresh
> im following cookie storage for refresh and access token 7days and 15 min expiry with
> flow that once user logins -> verify cookie token - > send user data to fronend with checkauth endpoint
> -> logout endpoint and register endpoint and refresh endpoint for react vite
> react vite im going to use zustand store
> react service folder and store folder
> in service api.js file with interceptor and refresh call and api calls in authStore and all stores based
> also give me why we are not following localstorage and session rather we are doing cookie article with
> in my above auth backend create secret token jwt refresh and access and middleware to verify users
